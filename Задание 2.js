let num1 = +prompt ('введите число от 0 до 1000');
if (num < 1000) 
  function isPrime (num){
    for (let i= 2; i < num; i++) {
      if (num % i === 0) return false;     
    }
    return num !== 1;
  }
function printPrimes(1000){
  for (let i=2; i <=1000; i++) {
    if (isPrime(num)) == 'true')
  console.log (num + 'простое число');
  }
}
  else {
   console.log ('слишком большое число');
  }
